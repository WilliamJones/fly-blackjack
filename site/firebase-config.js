// Paste your Firebase web app config here (Firebase console -> Project settings
// -> Your apps -> SDK setup and configuration). These values are meant to be
// public; access is controlled by firestore.rules, not by hiding them.
// Leave this null and the page runs as a single-player table.
window.FIREBASE_CONFIG = null;
// window.FIREBASE_CONFIG = {
//   apiKey: "...", authDomain: "your-project.firebaseapp.com",
//   projectId: "your-project", appId: "1:...:web:...",
// };
